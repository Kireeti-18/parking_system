export type VehicleDepart = {
  user_identification: string;
  is_diff_user: boolean;
  vehicle_type: string;
  depart_code: string;
  slot: number;
  register: boolean;
};

export type vehicleDepart = VehicleDepart;
